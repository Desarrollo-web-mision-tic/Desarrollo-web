import React from 'react';

const Logout = () => {
   
    return (
        <>
        <form >
            <button type='submit'>Logout</button>
        </form>
        </>
    );
};

export default Logout;